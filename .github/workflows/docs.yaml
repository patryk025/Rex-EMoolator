name: docs

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - "tools/pygments-cnv/**"
      - "assets/test-assets/scripts/Arrajki.cnv"
      - "mkdocs.yml"
      - ".github/workflows/docs.yaml"
  pull_request:
    paths:
      - "docs/**"
      - "tools/pygments-cnv/**"
      - "assets/test-assets/scripts/Arrajki.cnv"
      - "mkdocs.yml"
      - ".github/workflows/docs.yaml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: docs/requirements.txt

      - name: Install MkDocs and plugins
        run: pip install -r docs/requirements.txt

      - name: Test CNV syntax highlighting
        run: python -m unittest discover -s tools/pygments-cnv/tests -v

      - name: Build site
        run: mkdocs build

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
